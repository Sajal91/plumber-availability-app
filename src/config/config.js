const API_END_POINT = process.env.API_END_POINT;
const PORT = 5000;

export const API_BASE_URL = `http://${API_END_POINT}:${PORT}/api`;
export const SOCKET_URL = `http://${API_END_POINT}:${PORT}`;